import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nursery.app',
  appName: 'বৃক্ষরোপণ ট্র্যাকার (Plantation Tracker)',
  webDir: 'dist'
};

export default config;
