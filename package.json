{
  "name": "pmis-v2",
  "private": true,
  "version": "2.0.0",
  "packageManager": "pnpm@9.12.0",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*",
    "workers/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "~5.8.2",
    "@types/node": "^22.20.1"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
